---
/**
 * Hülle für die drei Look-Varianten. Bewusst OHNE global.css: die bestehende
 * Designsprache ist trocken und evidenzgetrieben, also genau das, wovon die
 * Persona-Seite wegwill. Jede Variante bringt ihre Tokens selbst mit.
 *
 * Alles unter /look/ ist Wegwerfware zum Anschauen: noindex, nicht verlinkt,
 * fliegt raus, sobald eine Variante gewonnen hat.
 */
// Commit Mono kommt sonst aus global.css, die hier bewusst fehlt. Als Import
// im Frontmatter statt als @import im Style-Block: nur so löst Vite die
// Font-Pfade des Pakets zur Bauzeit auf.
import '@fontsource/commit-mono/latin-400.css';
import '@fontsource/commit-mono/latin-500.css';
import { palettes, defaultPalette } from '../data/lookPalettes';

interface Props {
  title: string;
}
const { title } = Astro.props;
const here = Astro.url.pathname.replace(/\/$/, '') || '/look';
---

<!doctype html>
<html lang="de-AT">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <title>{title}</title>
    <style is:global>
      @font-face {
        font-family: 'Clash Display';
        src: url('../assets/fonts/clash-display-variable.woff2') format('woff2');
        font-weight: 200 700;
        font-display: swap;
      }
      @font-face {
        font-family: 'Panchang';
        src: url('../assets/fonts/panchang-variable.woff2') format('woff2');
        font-weight: 200 800;
        font-display: swap;
      }
      @font-face {
        font-family: 'Supreme';
        src: url('../assets/fonts/supreme-variable.woff2') format('woff2');
        font-weight: 100 800;
        font-display: swap;
      }
      @font-face {
        font-family: 'Switzer';
        src: url('../assets/fonts/switzer-variable.woff2') format('woff2');
        font-weight: 100 900;
        font-display: swap;
      }
      @font-face {
        font-family: 'General Sans';
        src: url('../assets/fonts/general-sans-variable.woff2') format('woff2');
        font-weight: 200 700;
        font-display: swap;
      }

      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }
      html {
        -webkit-text-size-adjust: 100%;
      }
      body {
        margin: 0;
      }
      img {
        display: block;
        max-width: 100%;
      }

      @media (prefers-reduced-motion: reduce) {
        *,
        *::before,
        *::after {
          animation-duration: 0.01ms !important;
          animation-iteration-count: 1 !important;
          transition-duration: 0.01ms !important;
        }
      }

      /* Die Dev-Toolbar sitzt sonst auf jedem Screenshot mitten im Entwurf. */
      astro-dev-toolbar {
        display: none !important;
      }

      /* Schaltleiste zwischen den Varianten. Gehört nicht zum Entwurf,
         deshalb neutral, klein und oben — fixiert hätte sie auf jedem
         Vollseiten-Screenshot quer über der Kachelreihe gelegen. */
      .variant-switch {
        display: flex;
        justify-content: center;
        gap: 0.25rem;
        padding: 0.5rem;
        font: 500 0.75rem/1 ui-monospace, monospace;
        background: color-mix(in oklch, #1b1b1f 88%, transparent);
        color: #f2ede4;
      }
      .variant-switch a {
        padding: 0.4rem 0.7rem;
        color: inherit;
        text-decoration: none;
        border: 1px solid transparent;
      }
      .variant-switch a:hover {
        border-color: currentColor;
      }
      .variant-switch a[aria-current='page'] {
        background: #f2ede4;
        color: #1b1b1f;
      }
      .variant-switch-sep {
        width: 1px;
        margin: 0.15rem 0.5rem;
        background: currentColor;
        opacity: 0.35;
      }
    </style>
  </head>
  <body>
    <nav class="variant-switch" aria-label="Look-Varianten">
      {
        palettes.map((p) => (
          <a
            href={`/look/a/${p.id}`}
            aria-current={here === `/look/a/${p.id}` || (p.id === defaultPalette.id && here === '/look/a') ? 'page' : undefined}
          >
            A · {p.name}
          </a>
        ))
      }
      <span class="variant-switch-sep" aria-hidden="true"></span>
      <a href="/look/b" aria-current={here === '/look/b' ? 'page' : undefined}>B · Kladde</a>
      <a href="/look/c" aria-current={here === '/look/c' ? 'page' : undefined}>C · Anzeigetafel</a>
      <a href="/look" aria-current={here === '/look' ? 'page' : undefined}>Übersicht</a>
    </nav>
    <slot />
  </body>
</html>
