---
import LanguageToggle from './LanguageToggle.astro';
import { t, type Locale, getLocalizedPath } from '../i18n/util';

export interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const home = getLocalizedPath(locale, '/');
const navLinks = [
  { href: `${home}#work`, label: t(locale, 'nav.work') },
  { href: `${home}#stack`, label: t(locale, 'nav.stack') },
  { href: `${home}#contact`, label: t(locale, 'nav.contact') },
];
---
<header class="site-header">
  <div class="container header-inner">
    <a href={home} class="site-mark" rel="home">
      <span class="mark-name">Benjamin Nößler</span>
      <span class="mark-sep" aria-hidden="true">/</span>
      <span class="mark-tag">{locale === 'de' ? 'Fullstack, Wien' : 'Fullstack, Vienna'}</span>
    </a>

    <nav aria-label="Primary" class="site-nav">
      <ul>
        {navLinks.map((link) => (
          <li><a href={link.href}>{link.label}</a></li>
        ))}
      </ul>
    </nav>

    <LanguageToggle currentLocale={locale} />
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: var(--color-bg);
    border-bottom: 1px solid var(--color-rule);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-5);
    padding-block: var(--space-3);
  }

  a.site-mark,
  a.site-mark:hover {
    text-decoration: none;
  }
  .site-mark {
    display: inline-flex;
    align-items: baseline;
    gap: var(--space-2);
    color: var(--color-text);
    white-space: nowrap;
    min-width: 0;
  }
  .site-mark:hover {
    color: var(--color-text);
  }
  .site-mark:hover .mark-tag {
    color: var(--color-accent);
  }

  .mark-name {
    font-family: var(--font-display);
    font-weight: 500;
    font-size: var(--fs-sm);
    letter-spacing: -0.01em;
    color: var(--color-text);
  }
  .mark-sep {
    font-family: var(--font-mono);
    font-size: var(--fs-xs);
    color: var(--color-text-subtle);
  }
  .mark-tag {
    font-family: var(--font-mono);
    font-size: var(--fs-xs);
    letter-spacing: 0.03em;
    color: var(--color-text-muted);
    transition: color var(--transition);
  }

  @media (max-width: 40rem) {
    .mark-sep,
    .mark-tag {
      display: none;
    }
  }

  .site-nav ul {
    display: flex;
    gap: var(--space-5);
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .site-nav a {
    font-size: var(--fs-sm);
    color: var(--color-text-muted);
    text-decoration: none;
    padding-block: var(--space-1);
    border-bottom: 1px solid transparent;
    transition: color var(--transition), border-color var(--transition);
  }
  .site-nav a:hover {
    color: var(--color-text);
    border-bottom-color: var(--color-accent);
    text-decoration: none;
  }

  @media (max-width: 34rem) {
    .header-inner {
      flex-wrap: wrap;
      row-gap: var(--space-2);
    }
    .site-nav {
      order: 3;
      flex-basis: 100%;
      padding-block-start: var(--space-2);
      border-top: 1px solid var(--color-rule);
    }
    .site-nav ul {
      gap: var(--space-4);
    }
    .site-nav a {
      font-family: var(--font-mono);
      font-size: var(--fs-xs);
      letter-spacing: 0.06em;
      text-transform: uppercase;
    }
  }
</style>
